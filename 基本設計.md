# EEV アプリ 基本設計書

## 1. アーキテクチャ方針
本アプリは **MVVM (Model-View-ViewModel)** および **単方向データフロー (Unidirectional Data Flow)** パターンを採用し、UI（Jetpack Compose）、ビジネスロジック（ViewModel）、データソース（Repository / Storage）を明確に分離します。
また、将来的な機能追加およびモジュール独立性を向上させるため、**Core/Plugin アーキテクチャ** を採用します。

## 2. システム構成・コンポーネント設計

```mermaid
graph TD
    App UI[Jetpack Compose UI Layer] --> ViewModel[ViewModel Layer]
    ViewModel --> CoreRepo[Core Repository / Storage API]
    ViewModel --> DataStore[DataStore Manager]
    ViewModel --> PluginRegistry[Plugin Registry]
    
    PluginRegistry --> ViewerPlugin[Viewer Plugin]
    PluginRegistry --> TranslationPlugin[Translation Plugin]
    PluginRegistry --> ThumbnailPlugin[Thumbnail Plugin]
    
    TranslationPlugin --> MLKitOCR[Google ML Kit Text Recognition]
    TranslationPlugin --> MLKitTrans[Google ML Kit Translation]
    ThumbnailPlugin --> CoilCache[Coil Image Loader Cache]
```

### 2.1 Core（基盤モジュール）
* **Explorer Module**: ストレージ内のファイル・フォルダ構造を取得し、UIへ配信。名前・日付・画像数での並び替えソート処理を保持。サムネイルサイズ（大・中・小）および付加情報表示の切替、フォルダ移動履歴による復帰機能を提供。
* **DataStore Module**: 設定値（漫画モードON/OFF、翻訳言語設定、ソート順、サムネイルサイズ、情報表示）の非同期読み書きを担当。
* **Plugin Registry**: 起動時またはイベント発生時にプラグインの呼び出しおよびライフサイクルを統括。

### 2.2 Plugin（プラグインモジュール）
* **Viewer Plugin**:
  * 全画面画像表示およびジェスチャ検出（シングルタップ、長押し、トリプルタップ）。
  * トリプルタップ時に操作選択モーダル（ビューワ終了 / 次のフォルダへ移動 / 先頭画像へ戻る）を表示。
  * 漫画モード時、画像のクロップ/Viewport制御（右半分表示・左半分表示）。
  * 下端スワイプによる BottomSheet UI のトリガー。
* **Translation Plugin**:
  * ビューワに読み込まれたビットマップ画像から文字座標と文字列をインメモリ抽出。
  * オフライン翻訳エンジンに渡し、オーバーレイビュー描画用モデルを生成。
* **Thumbnail Plugin**:
  * フォルダ探索時に非同期で先頭画像を特定し、Coil経由で縮小画像を生成・メモリ/ディスクキャッシュ。
  * `FolderThumbnailCache` により `cacheDir/folder_thumbs` へ軽量画像（最大辺256px）を保存し、LRU方式（上限50MB）で容量管理する。

### 2.3 ストレージアクセス方針

* **全ファイルアクセス**: Android 11 (API 30) 以降では `MANAGE_EXTERNAL_STORAGE` を要求し、未許可時は設定画面へ誘導する（`MainActivity.requestAllFilesAccessIfNeeded()`）。
* **アプリ専用フォルダ**: 権限不要でアクセス可能な `filesDir` を、未許可時の初期表示ディレクトリとして提供する（`MainActivity.resolveInitialDirectory()`）。
* **初期ディレクトリ決定**: 全ファイルアクセス許可時は外部ストレージ直下、未許可時はアプリ専用フォルダを `ExplorerViewModel` の `initialDirectory` に渡す。
* **権限許可後の自動切替**: 設定画面から戻った際に `MainActivity.onResume()` が初期ディレクトリを再解決し、許可済みなら外部ストレージ直下へ自動的に切り替える。
* **フォルダ移動履歴**: 権限のないフォルダへ入り込んで戻れなくなる問題に対し、`ExplorerViewModel` が `directoryHistory`（絶対パス）を保持し、トップバーの戻るボタン（`navigateBack()`）で直前のフォルダへ復帰できるようにする。

### 2.4 エクスプローラのスクロール位置保存と表示パフォーマンス

* フォルダパスをキーとした先頭表示インデックスを DataStore の `explorer_scroll_positions` に永続化する。
* `ExplorerScreen` は `LazyListState` / `LazyGridState` の `firstVisibleItemIndex` を `snapshotFlow` で監視し、フォルダごとに保存・復元する。
* **保存頻度の抑制**: `snapshotFlow` に `distinctUntilChanged()` と `debounce(300ms)` を適用し、スクロール停止時のみ DataStore へ書き込む。これにより書き込み連打 → Flow 再発行 → 再コンポーズの連鎖を防ぐ。
* **読み込み表示の限定**: `isLoading` による全画面の読み込み表示は「一覧が未取得（`filteredFileItems` が空）」の場合に限定する。一覧表示中は `isLoading` が再び `true` になってもリストを破棄しないため、`LazyListState` が維持されスクロール位置が失われない。
* **重複ロードの排除**: `sortOrder` / `showHiddenFiles` の DataStore 初回購読通知では再ロードせず、値が実際に変化した場合のみ `loadDirectory` を実行する（`isInitialLoadDone` フラグ）。
* **メタデータのバッチ反映**: フォルダのサムネイル・画像数は `METADATA_BATCH_SIZE`（8件）ごと、および完了時に一括反映し、1件ごとの StateFlow 更新による再コンポーズ多発を抑える。

### 2.5 ソート順の唯一の真実源（Single Source of Truth）

多重ソートによる不整合を防ぐため、ビューワのソート順決定は以下の原則に統一する。

* **決定の唯一の真実源**: `ViewerViewModel.loadImagesFromDirectory(directory, initialFile, sortOrder)` の引数 `sortOrder`。
* **一覧取得**: 上記引数 `sortOrder` を用いて `FileStorageManager.getImageFiles()` を呼び出す。
* **表示用写像**: `ViewerUiState.viewerSortOrder` は確定値を保持するのみで、読み込み時の決定には使用しない。
* **永続化**: DataStore の `viewer_sort_order` は次回起動時のデフォルト値であり、現在の表示順決定には関与しない。`updateViewerSortOrder()` は永続化のみを行う。
* **再ソート**: DataStore 値の購読は state と異なる場合のみ `resortCurrentImages()` を実行し、`anchorFilePath`（起点画像）を基準に表示位置を維持する。
* **エクスプローラとの分離**: ビューワのソート順はエクスプローラのソート順（`explorerSortOrder`）とは独立して管理する。フォルダまたぎ移動（次/前フォルダ）の並び順も `viewerSortOrder` を基準とし、ビューワ設定画面（`ViewerBottomSheet`）で日付/名前×昇順/降順を選択できる。

### 2.6 エクスプローラの表示オプション

* **サムネイルサイズ**: `ThumbnailSize`（`SMALL` / `MEDIUM` / `LARGE`）を DataStore の `thumbnail_size` に永続化し、リスト表示のサムネイル寸法およびグリッド表示の列数・寸法に反映する。
* **付加情報表示**: `show_item_info` が ON の場合、一覧項目に更新日時（フォルダは画像枚数も）を表示する。表示オプションは `DisplayOptionDialog` から変更する。

## 3. フォルダ構成ルール（機能単位 / Feature-Based）
コードは役割ごとのレイヤー分けではなく、機能ごとの凝集度を高めるため**機能単位（Feature-Based）**に厳格に整理する。

```text
com.example.eev/
├── core/                   # 共通基盤レイヤー
│   ├── datastore/          # EEVDataStore.kt (設定保存)
│   ├── model/              # FileItem.kt, SortOrder.kt, Language.kt, ViewMode.kt, ThumbnailSize.kt
│   ├── storage/            # FileStorageManager.kt
│   └── ui/                 # AppTheme.kt, CommonComponents.kt
├── feature/                # 機能モジュールレイヤー (Feature-Based)
│   ├── explorer/           # エクスプローラ (ExplorerScreen.kt, ExplorerViewModel.kt, SortOptionDialog.kt, DisplayOptionDialog.kt)
│   ├── viewer/             # 画像ビューワ (ViewerScreen.kt, ViewerViewModel.kt, ViewerBottomSheet.kt, ViewerTripleTapDialog.kt)
│   ├── translation/        # 画像内翻訳 (OcrEngine.kt, TranslationEngine.kt, TranslationOverlay.kt)
│   └── thumbnail/          # サムネイル (ThumbnailGenerator.kt, FolderThumbnailCache.kt)
├── plugin/                 # プラグイン設計レイヤー
│   ├── EEVPlugin.kt        # プラグイン共通インターフェース
│   └── PluginRegistry.kt   # プラグイン登録・ライフサイクル管理
└── settings/               # 設定画面 (SettingsScreen.kt, SettingsViewModel.kt)
```

## 4. デザイン要件
* **アプリアイコン**: ファイルエクスプローラを連想させるフォルダーアイコンと画像ピクトグラムを融合したモダンなフラットデザイン。
* **テーマ**: Modern Minimal Dark / Light テーマをサポート。

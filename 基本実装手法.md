# 基本実装手法 (Implementation Guidelines & Knowledge Base)

本ドキュメントは、プロジェクト「EEV」の開発・実装において確立された実装ノウハウ、動作確認済みのライブラリ設定、およびデバッグ時に得られた注意点・アンチパターンを記録・共有するためのドキュメントです。開発進行に伴い随時更新されます。

---

## 1. 動作確認済み推奨ライブラリ・仕様

### 1.1 画像ロード・サムネイル表示 (`Coil`)
* **ライブラリ**: `io.coil-kt:coil-compose:2.x`
* **使用手法**: 
  * Jetpack Composeの `AsyncImage` または `rememberAsyncImagePainter` を使用。
  * サムネイル生成時は `size(width, height)` と `scale(Scale.FIT)` を指定し、メモリ消費量を抑えてデコードする。

### 1.2 OCR・オンデバイス翻訳 (`Google ML Kit`)
* **ライブラリ**: 
  * `com.google.mlkit:text-recognition-japanese:16.x` (日本語・英語・中国語認識)
  * `com.google.mlkit:translate:17.x` (オンデバイス言語翻訳)
* **モデル管理**:
  * 翻訳用モデル (`TranslateRemoteModel`) は初回利用時にダウンロードが必要なため、ダウンロード進捗UIのハンドリングを行うこと。

### 1.3 設定情報の保存 (`Jetpack DataStore`)
* **ライブラリ**: `androidx.datastore:datastore-preferences:1.x`
* **使用手法**:
  * SharedPreferences ではなく Datastore Flow を使用し、`ViewModel` で非同期StateFlowにマッピングしてUIに配信する。

---

## 2. 実装上の注意点・アンチパターン（知見共有）

### 2.1 ストレージアクセス・パーミッション
* **注意点**: Android 11 (API 30) 以降での全ファイルアクセス権限 (`MANAGE_EXTERNAL_STORAGE`) または MediaStore API / SAF (Storage Access Framework) の扱い。
* **対策**: 画像閲覧メインのため、可能な限り MediaStore / SAF アクセスで完結させ、必要な場合のみ適切な権限ダイアログを実装する。

### 2.2 漫画モード（画像分割・拡大表示）の描画パフォーマンス
* **注意点**: 高解像度画像をそのまま `Canvas` で左右に分割クロップ描画すると OOM (Out Of Memory) や再描画遅延が発生する可能性がある。
* **対策**: `SubsamplingScaleImageView` または Coil のデコードオプションを活用し、画面解像度に合わせた適切なサブサンプリング表示を行う。

### 2.3 ML Kit OCR座標とCompose描画座標の変換
* **注意点**: ML Kit から返される `BoundingBox` の座標は画像自体の元解像度基準であるのに対し、Compose UIの画面解像度とアスペクト比調整（`ContentScale.Fit`）により実際の画面上の位置とずれる場合がある。
* **対策**: 画像の表示倍率とオフセット値を計算する `MatrixTransformHelper` を作成し、正確なオーバーレイ描画を行う。

---

## 3. 更新履歴・メンテナンスログ
* **2026-08-30**: 初版作成（開発開始・仕様策定段階の初期ノウハウ追加）

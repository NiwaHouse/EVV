# EEV アプリ 要件定義書

## 1. システム概要
「EEV」は、Android端末上で動作する高速かつ高機能なファイルエクスプローラ兼画像ビューワアプリケーションです。
一般的なファイル閲覧機能に加え、漫画閲覧に適した「漫画モード（見開き表示）」、画像内の文字をオンデバイスで自動認識・翻訳してオーバーレイ表示する「画像内翻訳機能」、および画像フォルダの「高速サムネイル表示機能」をプラグイン構造として提供します。

## 2. 動作環境・仕様フレームワーク
* **対象OS**: Android 5.0 (API Level 21) 以上
* **主要フレームワーク**: Kotlin + Jetpack Compose
* **デザインパターン**: MVVM (Model-View-ViewModel / 単方向データフロー)
* **アプリアイコン**: ファイルエクスプローラをモチーフとしたデザイン（フォルダと画像を象徴するデザイン）

## 3. 主要機能要件

### 3.1 Core（ファイルエクスプローラ機能）
* **ファイル・フォルダブラウジング**: ディレクトリ構造のナビゲーション機能。
* **ソート（並び順）切替機能**:
  * 名前順 (Name)
  * 日付順 (Date)
  * 画像数順 (Image Count)
  * ※エクスプローラのソート順とビューワのソート順は独立して管理する。
  * ※ビューワのソート順は `loadImagesFromDirectory` の引数を唯一の真実源とし、DataStore 値との二重決定による不整合を生じさせない。
* **表示オプション切替機能**:
  * サムネイルサイズ3段階（大・中・小）を選択可能。グリッド表示の列数・寸法にも連動する。
  * 日付・枚数などの付加情報表示のON/OFFチェック。
* **フォルダ移動履歴・戻る機能**: 権限のないフォルダへ入り込んだ場合でも、トップバーの戻るボタンで直前のフォルダへ復帰できる。
* **設定画面連携**: トップバーのアイコンからアプリ全体設定画面へ遷移。

### 3.2 Plugin: サムネイル生成機能
* **フォルダサムネイル生成**: フォルダ内の1番目の画像を検索・自動縮小し、フォルダアイコン上にサムネイル表示。
* **キャッシュ管理**: `cacheDir/folder_thumbs` に軽量画像（最大辺256px）を非同期キャッシュし、LRU方式（上限50MB）で容量を管理。次回以降の高速描画を実現。

### 3.3 Plugin: 画像ビューワ機能
* **画像表示モード**:
  * **通常モード**: 画像全体を表示。
  * **漫画モード (見開き表示)**: 1枚の画像を右半分・左半分に自動分割・拡大表示。右から左の順で読み進める。
* **ジェスチャ操作**:
  * **シングルタップ**: 次へ（右半分 → 左半分 → 次の画像の右半分... フォルダ末尾達成時は次のフォルダの先頭画像へ自動移動）。
  * **長押し**: 前へ（直前の画像/直前の分割領域へ戻る）。
  * **トリプルタップ**: 操作選択モーダルを表示。選択肢は「画像ビューワの終了」「次のフォルダに移動（移動先の先頭画像を自動表示）」「今のフォルダの最初の画像に戻る（ビューワは開いたまま）」。
  * ※ダブルタップは無効化し、誤操作によるビューワ終了を防止する。
* **ボトムシート設定UI**:
  * 画面下端からのスワイプ操作で出現。
  * 漫画モード ON/OFF 切替、翻訳言語の変更を即座に適用可能。
  * ビューワ独自の並び順（日付/名前 × 昇順/降順）を選択可能。フォルダ内画像の表示順および「次のフォルダ選択基準」に適用される。

### 3.4 Plugin: 画像内翻訳機能
* **OCR文字抽出**: Google ML Kit Text Recognition により画像内のテキスト領域を検出。
* **オンデバイス翻訳**: Google ML Kit On-Device Translation により、オフラインで言語翻訳を実行。
* **対応言語**: 日本語、英語、中国語（翻訳元は自動検出はサポートしない）。
* **オーバーレイ描画**: 原本の文字位置に合わせて翻訳テキストを画像上に自然にオーバーレイ表示。

### 3.5 状態・設定管理
* **Jetpack DataStore (Preferences)** による永続化：
  * 漫画モード ON/OFF 設定
  * 翻訳元言語・翻訳先言語設定
  * 翻訳の　ON/OFF設定
  * エクスプローラの選択ソート順
  * ビューワ独自の選択ソート順
  * サムネイルサイズ（大・中・小）
  * 付加情報（日付・枚数）表示 ON/OFF
  * フォルダごとのエクスプローラスクロール位置
* **ViewModel / StateFlow** によるリアルタイム状態配信。

### 3.6 ストレージアクセス・権限要件
* **全ファイルアクセス権限**: Android 11 (API 30) 以降では `MANAGE_EXTERNAL_STORAGE` を要求し、未許可時は設定画面へ誘導する。個人利用/サイドロード配信を前提とする。
* **アプリ専用フォルダ**: 権限不要でアクセス可能なアプリ専用フォルダ（`filesDir`）を、未許可時の初期表示ディレクトリとして提供する。
* **初期表示**: 全ファイルアクセス許可時は外部ストレージ直下、未許可時はアプリ専用フォルダを初期表示する。
* **権限許可後の自動切替**: 設定画面から戻った際に初期ディレクトリを再解決し、許可済みなら外部ストレージ直下へ自動的に切り替える。
* **フォルダ移動履歴**: 権限のないフォルダへ入り込んで戻れなくなる問題に対し、フォルダ移動履歴スタックと戻るボタンで直前のフォルダへ復帰できるようにする。

## 4. フォルダ構成ルール（機能単位 / Feature-Based）
プロジェクト構造は、機能ごとの独立性と変更容易性を保つため、以下の**機能単位（Feature-Based）**構成を厳格に適用する。

```text
com.example.eev/
├── core/                   # 全体共通基盤 (ストレージ、共通UI、ドメインモデル)
│   ├── datastore/          # Preferences (設定保存)
│   ├── model/              # 共通モデル (SortOrder, Language, FileItem, ViewMode, ThumbnailSize)
│   ├── storage/            # MediaStore / ストレージアクセスAPI
│   └── ui/                 # 共通テーマ・コンポーネント
├── feature/                # 機能モジュール群 (Feature-Based)
│   ├── explorer/           # エクスプローラ画面 (ExplorerScreen, ViewModel, SortOptionDialog, DisplayOptionDialog)
│   ├── viewer/             # 画像ビューワ画面 (ViewerScreen, ViewModel, Gesture, BottomSheet, TripleTapDialog)
│   ├── translation/        # OCR・翻訳機能 (OcrEngine, Translator, OverlayView)
│   └── thumbnail/          # サムネイル生成・キャッシュ管理機能 (ThumbnailGenerator, FolderThumbnailCache)
├── plugin/                 # プラグインアーキテクチャ基盤
│   ├── EEVPlugin.kt        # プラグイン共通インターフェース
│   └── PluginRegistry.kt   # プラグイン登録・ライフサイクル管理
└── settings/               # 全体設定画面 (SettingsScreen, ViewModel)
```
